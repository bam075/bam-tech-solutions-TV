<!DOCTYPE html>
<html lang="sw">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>BAM TECH SOLUTIONS TV</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;600;700;800;900&family=Inter:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/hls.js/1.5.13/hls.min.js"></script>
<style>
:root{
  --bg:#020409; --bg2:#080d1c; --bg3:#0d1428;
  --gold:#c9a04d; --gold2:#f0cf86; --gold3:#7a5c1e;
  --ice:#4fd1ff; --signal:#ff4757; --green:#2ed573;
  --text:#eef1f8; --text2:#a8b3cc; --text3:#4a5568;
  --border:rgba(201,160,77,0.18); --border2:rgba(255,255,255,0.06);
  --r:14px;
}
*{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
body{background:var(--bg);color:var(--text);font-family:'Inter',sans-serif;min-height:100vh;overflow-x:hidden;}
body::before{content:'';position:fixed;inset:0;pointer-events:none;z-index:9999;
  background:repeating-linear-gradient(0deg,rgba(0,0,0,0.03) 0px,rgba(0,0,0,0.03) 1px,transparent 1px,transparent 2px);}
#glow{position:fixed;inset:0;pointer-events:none;z-index:0;
  background:radial-gradient(700px circle at var(--mx,50%) var(--my,30%),
    rgba(201,160,77,0.12) 0%, rgba(79,209,255,0.06) 35%, transparent 65%);}
.page{position:relative;z-index:1;min-height:100vh;display:flex;flex-direction:column;}

/* TOPBAR */
.topbar{display:flex;align-items:center;justify-content:space-between;
  padding:0 28px;height:64px;border-bottom:1px solid var(--border);
  background:linear-gradient(180deg,rgba(8,13,28,0.97),rgba(8,13,28,0.8));
  backdrop-filter:blur(12px);position:sticky;top:0;z-index:100;flex-wrap:wrap;gap:8px;}
.logo{display:flex;align-items:center;gap:12px;text-decoration:none;}
.logo-badge{width:40px;height:40px;border-radius:10px;flex-shrink:0;
  background:linear-gradient(135deg,var(--gold3),var(--gold),var(--gold2));
  display:flex;align-items:center;justify-content:center;
  font-family:'Sora',sans-serif;font-weight:900;color:#020409;font-size:15px;
  box-shadow:0 0 24px rgba(201,160,77,0.45);position:relative;overflow:hidden;}
.logo-badge::after{content:'';position:absolute;inset:0;
  background:linear-gradient(135deg,rgba(255,255,255,0.35),transparent 50%);}
.logo-text .main{font-family:'Sora',sans-serif;font-size:15px;font-weight:800;letter-spacing:0.03em;}
.logo-text .main em{font-style:normal;color:var(--gold2);}
.logo-text .sub{font-size:9.5px;letter-spacing:0.18em;color:var(--text2);text-transform:uppercase;}
.topbar-right{display:flex;align-items:center;gap:20px;}
.live-pill{display:flex;align-items:center;gap:6px;background:rgba(255,71,87,0.12);
  border:1px solid rgba(255,71,87,0.35);border-radius:20px;padding:5px 12px;
  font-family:'Sora',sans-serif;font-size:11px;font-weight:700;letter-spacing:0.12em;color:var(--signal);}
.live-dot{width:7px;height:7px;border-radius:50%;background:var(--signal);
  box-shadow:0 0 8px var(--signal);animation:blink 1.4s ease-in-out infinite;}
@keyframes blink{0%,100%{opacity:1;}50%{opacity:0.2;}}
.viewers{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--text2);font-family:'JetBrains Mono',monospace;}
.clock-box{font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--text2);
  background:rgba(255,255,255,0.04);border:1px solid var(--border2);border-radius:8px;padding:6px 10px;}

/* HERO */
.hero{text-align:center;padding:36px 20px 20px;
  background:radial-gradient(ellipse 80% 200px at 50% 0%,rgba(201,160,77,0.07),transparent);
  border-bottom:1px solid var(--border);}
.hero .eyebrow{display:inline-block;font-family:'JetBrains Mono',monospace;font-size:10px;
  letter-spacing:0.22em;color:var(--gold);border:1px solid var(--border);border-radius:20px;
  padding:4px 14px;margin-bottom:14px;text-transform:uppercase;}
.hero h1{font-family:'Sora',sans-serif;font-size:clamp(22px,5vw,48px);font-weight:900;
  line-height:1.1;letter-spacing:-0.02em;margin-bottom:8px;
  background:linear-gradient(135deg,var(--gold2) 0%,#fff 50%,var(--ice) 100%);
  -webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;}
.hero p{font-size:13px;color:var(--text2);letter-spacing:0.03em;}

/* MAIN GRID */
.main-grid{flex:1;display:grid;grid-template-columns:1fr 320px;gap:0;min-height:0;}
@media(max-width:860px){.main-grid{grid-template-columns:1fr;}}

/* VIDEO COL */
.video-col{padding:24px;display:flex;flex-direction:column;gap:16px;border-right:1px solid var(--border2);}
.monitor{position:relative;border-radius:var(--r);overflow:hidden;background:#000;
  aspect-ratio:16/9;border:1px solid var(--border);
  box-shadow:0 0 0 1px rgba(255,255,255,0.03),0 40px 80px -20px rgba(0,0,0,0.9),0 0 80px -20px rgba(201,160,77,0.12);}
video{width:100%;height:100%;display:block;background:#000;}
.monitor-overlay{position:absolute;inset:0;display:flex;flex-direction:column;
  align-items:center;justify-content:center;gap:14px;background:rgba(2,4,9,0.88);
  font-family:'Sora',sans-serif;transition:opacity 0.4s;}
.monitor-overlay.gone{opacity:0;pointer-events:none;}
.loader-ring{width:44px;height:44px;border-radius:50%;border:2px solid rgba(201,160,77,0.2);
  border-top-color:var(--gold);animation:spin 0.85s linear infinite;}
@keyframes spin{to{transform:rotate(360deg);}}
.ov-title{font-size:14px;font-weight:600;color:var(--text2);}
.ov-sub{font-size:11px;color:var(--text3);}
.channel-bug{position:absolute;top:12px;left:12px;z-index:2;display:flex;align-items:center;gap:7px;
  background:rgba(2,4,9,0.72);border:1px solid var(--border);border-radius:8px;
  padding:6px 10px;backdrop-filter:blur(6px);opacity:0;transition:opacity 0.6s 0.8s;}
.channel-bug.show{opacity:1;}
.bug-dot{width:6px;height:6px;border-radius:50%;background:var(--signal);
  box-shadow:0 0 6px var(--signal);animation:blink 1.4s infinite;}
.bug-label{font-family:'Sora',sans-serif;font-size:10px;font-weight:700;letter-spacing:0.1em;color:var(--gold2);}

.now-bar{display:flex;align-items:center;gap:14px;background:var(--bg2);
  border:1px solid var(--border);border-radius:var(--r);padding:14px 18px;}
.now-icon{width:38px;height:38px;border-radius:9px;flex-shrink:0;
  background:linear-gradient(135deg,var(--gold3),var(--gold));
  display:flex;align-items:center;justify-content:center;font-size:18px;}
.now-label{font-size:9.5px;letter-spacing:0.18em;color:var(--gold);margin-bottom:3px;font-family:'JetBrains Mono',monospace;}
.now-title{font-family:'Sora',sans-serif;font-size:14px;font-weight:600;color:var(--text);}
.now-time{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text2);margin-top:2px;}

.prog-wrap{background:var(--bg2);border:1px solid var(--border);border-radius:var(--r);padding:14px 18px;}
.prog-head{display:flex;justify-content:space-between;font-size:11px;color:var(--text2);margin-bottom:8px;}
.prog-pct{color:var(--gold);font-family:'JetBrains Mono',monospace;}
.prog-track{height:3px;background:rgba(255,255,255,0.07);border-radius:3px;overflow:hidden;}
.prog-fill{height:100%;background:linear-gradient(90deg,var(--gold3),var(--gold2));border-radius:3px;transition:width 1s linear;width:0%;}

/* SIDEBAR */
.sidebar{display:flex;flex-direction:column;background:var(--bg2);}
.sidebar-section{border-bottom:1px solid var(--border2);padding:20px;}
.sidebar-title{font-family:'Sora',sans-serif;font-size:11px;font-weight:700;
  letter-spacing:0.15em;color:var(--gold);text-transform:uppercase;margin-bottom:14px;}
.ch-info{display:flex;flex-direction:column;gap:0;}
.ch-stat{display:flex;justify-content:space-between;align-items:center;
  padding:9px 0;border-bottom:1px solid var(--border2);}
.ch-stat:last-child{border:none;}
.ch-key{font-size:12px;color:var(--text2);}
.ch-val{font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--text);}
.status-online{color:var(--green)!important;}

.epg-list{display:flex;flex-direction:column;}
.epg-item{padding:10px 6px;border-bottom:1px solid var(--border2);display:flex;gap:12px;border-radius:8px;}
.epg-item:last-child{border:none;}
.epg-item.active{background:rgba(201,160,77,0.06);}
.epg-time{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--text2);flex-shrink:0;width:42px;padding-top:1px;}
.epg-name{font-size:13px;font-weight:500;color:var(--text);margin-bottom:2px;}
.epg-name.active-name{color:var(--gold2);}
.epg-desc{font-size:11px;color:var(--text3);line-height:1.4;}
.epg-tag{display:inline-block;font-size:9px;letter-spacing:0.1em;
  background:rgba(201,160,77,0.12);color:var(--gold);border-radius:4px;padding:2px 6px;margin-top:4px;}
.past{opacity:0.4;}

.share-row{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px;}
.share-btn{flex:1;min-width:80px;padding:9px 10px;border-radius:9px;border:1px solid var(--border);
  background:rgba(255,255,255,0.03);color:var(--text2);font-family:'Sora',sans-serif;
  font-size:11px;font-weight:600;cursor:pointer;transition:all 0.2s;text-align:center;letter-spacing:0.04em;}
.share-btn:hover{border-color:var(--gold);color:var(--gold);background:rgba(201,160,77,0.07);}
.copy-row{display:flex;gap:8px;}
.copy-input{flex:1;background:rgba(255,255,255,0.03);border:1px solid var(--border2);
  border-radius:8px;padding:8px 12px;color:var(--text2);font-family:'JetBrains Mono',monospace;font-size:10px;outline:none;}
.copy-btn{padding:8px 14px;background:linear-gradient(135deg,var(--gold3),var(--gold));
  border:none;border-radius:8px;color:#020409;font-weight:700;font-size:11px;
  cursor:pointer;font-family:'Sora',sans-serif;transition:opacity 0.2s;white-space:nowrap;}
.copy-btn:hover{opacity:0.85;}
.about-text{font-size:12px;color:var(--text2);line-height:1.7;}
.about-text strong{color:var(--gold2);}

footer{text-align:center;padding:18px;border-top:1px solid var(--border2);
  font-size:11px;color:var(--text3);letter-spacing:0.05em;}
footer strong{color:var(--gold);}

.toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%) translateY(60px);
  background:var(--bg3);border:1px solid var(--gold);border-radius:10px;
  padding:10px 22px;font-size:12px;font-family:'Sora',sans-serif;color:var(--gold2);
  z-index:9000;transition:transform 0.35s cubic-bezier(.34,1.56,.64,1);pointer-events:none;}
.toast.show{transform:translateX(-50%) translateY(0);}

@keyframes fadein{from{opacity:0;transform:translateY(14px);}to{opacity:1;transform:none;}}
.fi{animation:fadein 0.55s ease both;}
.fi1{animation-delay:0.05s;}.fi2{animation-delay:0.15s;}.fi3{animation-delay:0.25s;}

@media(max-width:640px){
  .topbar{padding:10px 16px;height:auto;}
  .viewers,.clock-box{display:none;}
  .hero{padding:24px 16px 16px;}
  .video-col{padding:14px;}
  .sidebar-section{padding:16px;}
}
</style>
</head>
<body>
<div id="glow"></div>
<div class="page">

  <div class="topbar">
    <a class="logo" href="#">
      <div class="logo-badge">BT</div>
      <div class="logo-text">
        <div class="main">BAM TECH <em>SOLUTIONS</em></div>
        <div class="sub">Television Network</div>
      </div>
    </a>
    <div class="topbar-right">
      <div class="live-pill"><span class="live-dot"></span>LIVE SASA</div>
      <div class="viewers">👁 <span id="viewer-count">—</span></div>
      <div class="clock-box" id="clock">--:--:-- EAT</div>
    </div>
  </div>

  <div class="hero fi">
    <div class="eyebrow">📡 &nbsp;Matangazo ya Moja kwa Moja</div>
    <h1>BAM TECH SOLUTIONS TV</h1>
    <p>Chaneli yako ya teknolojia, habari za kidijitali na burudani &mdash; Innovating Tanzania Digitally</p>
  </div>

  <div class="main-grid">

    <div class="video-col">
      <div class="monitor fi fi1">
        <video id="video" controls playsinline></video>
        <div class="channel-bug" id="channel-bug">
          <span class="bug-dot"></span>
          <span class="bug-label">BAM TECH TV</span>
        </div>
        <div class="monitor-overlay" id="overlay">
          <div class="loader-ring"></div>
          <div class="ov-title" id="ov-msg">Inaunganisha chaneli...</div>
          <div class="ov-sub" id="ov-sub">Subiri kidogo</div>
        </div>
      </div>

      <div class="now-bar fi fi2">
        <div class="now-icon">📺</div>
        <div>
          <div class="now-label">▶ INACHEZWA SASA</div>
          <div class="now-title" id="now-title">Inapakia taarifa...</div>
          <div class="now-time" id="now-time">&nbsp;</div>
        </div>
      </div>

      <div class="prog-wrap fi fi3">
        <div class="prog-head">
          <span id="prog-label">Ratiba</span>
          <span class="prog-pct" id="prog-pct">—</span>
        </div>
        <div class="prog-track"><div class="prog-fill" id="prog-fill"></div></div>
      </div>
    </div>

    <div class="sidebar">

      <div class="sidebar-section">
        <div class="sidebar-title">📊 Taarifa za Chaneli</div>
        <div class="ch-info">
          <div class="ch-stat"><span class="ch-key">Hali</span><span class="ch-val status-online">● HEWANI</span></div>
          <div class="ch-stat"><span class="ch-key">Watazamaji</span><span class="ch-val" id="stat-viewers">—</span></div>
          <div class="ch-stat"><span class="ch-key">Ubora</span><span class="ch-val">HD 720p</span></div>
          <div class="ch-stat"><span class="ch-key">Saa</span><span class="ch-val" id="stat-clock">—</span></div>
          <div class="ch-stat"><span class="ch-key">Lugha</span><span class="ch-val">Swahili / EN</span></div>
        </div>
      </div>

      <div class="sidebar-section">
        <div class="sidebar-title">📅 Ratiba ya Leo</div>
        <div class="epg-list" id="epg-list">
          <div style="font-size:12px;color:var(--text3)">Inapakia ratiba...</div>
        </div>
      </div>

      <div class="sidebar-section">
        <div class="sidebar-title">🔗 Shiriki Chaneli</div>
        <div class="share-row">
          <button class="share-btn" onclick="shareWA()">📲 WhatsApp</button>
          <button class="share-btn" onclick="copyLink()">🔗 Nakili Link</button>
        </div>
        <div class="copy-row">
          <input class="copy-input" id="share-input" type="text" readonly>
          <button class="copy-btn" onclick="copyLink()">Nakili</button>
        </div>
      </div>

      <div class="sidebar-section">
        <div class="sidebar-title">ℹ️ Kuhusu Chaneli</div>
        <div class="about-text">
          <strong>BAM TECH SOLUTIONS TV</strong> ni chaneli ya kidijitali inayotolewa na
          <strong>BAM Tech Solutions Companies Ltd</strong> &mdash; Tanzania.
          Tunatoa maudhui ya teknolojia, habari za kidijitali, mwongozo wa biashara na burudani.
          Kauli mbiu: <strong>Innovating Tanzania Digitally</strong>.
        </div>
      </div>

    </div>
  </div>

  <footer>&copy; 2026 <strong>BAM Tech Solutions Companies Ltd</strong> &nbsp;&middot;&nbsp; Dar es Salaam, Tanzania &nbsp;&middot;&nbsp; Haki zote zimehifadhiwa</footer>
</div>

<div class="toast" id="toast">✓ Imenakiliwa!</div>

<script>
// ================================================
//   WEKA LINK ZAKO HAPA (badilisha IP-YAKO)
// ================================================
const STREAM_URL = 'http://IP-YAKO/bamtv/stream.m3u8';
const EPG_URL    = 'epg.xml';
// ================================================

// Mouse glow
document.addEventListener('mousemove', e => {
  const g = document.getElementById('glow');
  g.style.setProperty('--mx', e.clientX + 'px');
  g.style.setProperty('--my', e.clientY + 'px');
});

// Clock
const pad = n => String(n).padStart(2,'0');
function tick() {
  const eat = new Date(new Date().toLocaleString('en-US',{timeZone:'Africa/Dar_es_Salaam'}));
  const s = `${pad(eat.getHours())}:${pad(eat.getMinutes())}:${pad(eat.getSeconds())} EAT`;
  document.getElementById('clock').textContent = s;
  document.getElementById('stat-clock').textContent = s.slice(0,5);
}
tick(); setInterval(tick,1000);

// Simulated viewers
let viewers = Math.floor(Math.random()*120)+40;
function updateViewers(){
  viewers = Math.max(20, viewers + Math.floor(Math.random()*7)-3);
  document.getElementById('viewer-count').textContent = viewers;
  document.getElementById('stat-viewers').textContent = viewers;
}
updateViewers(); setInterval(updateViewers,8000);

// Share
document.getElementById('share-input').value = window.location.href;
function copyLink(){
  navigator.clipboard.writeText(window.location.href)
    .then(()=>showToast('✓ Link imenakiliwa!'))
    .catch(()=>{ document.getElementById('share-input').select(); document.execCommand('copy'); showToast('✓ Link imenakiliwa!'); });
}
function shareWA(){
  window.open('https://wa.me/?text=Tazama%20BAM%20TECH%20SOLUTIONS%20TV%20sasa%20hivi%20%F0%9F%93%BA%0A'+encodeURIComponent(window.location.href),'_blank');
}
function showToast(m){
  const t=document.getElementById('toast');t.textContent=m;t.classList.add('show');
  setTimeout(()=>t.classList.remove('show'),2500);
}

// HLS Player
const video=document.getElementById('video');
const overlay=document.getElementById('overlay');
const bug=document.getElementById('channel-bug');
function hideOverlay(){overlay.classList.add('gone');bug.classList.add('show');}
function setOverlay(m,s){
  document.getElementById('ov-msg').textContent=m||'...';
  document.getElementById('ov-sub').textContent=s||'';
  overlay.classList.remove('gone');
}

if(Hls.isSupported()){
  const hls=new Hls({maxBufferLength:30});
  hls.loadSource(STREAM_URL);hls.attachMedia(video);
  hls.on(Hls.Events.MANIFEST_PARSED,()=>{hideOverlay();video.play().catch(()=>{});});
  hls.on(Hls.Events.ERROR,(e,d)=>{if(d.fatal)setOverlay('Tatizo la muunganiko','Tunajaribu tena...');});
}else if(video.canPlayType('application/vnd.apple.mpegurl')){
  video.src=STREAM_URL;
  video.addEventListener('loadedmetadata',hideOverlay);
}else{
  setOverlay('Browser haitumii stream hii','Tumia Chrome, Firefox au Safari');
}

// EPG
function parseXMLTV(s){
  if(!s)return null;
  const[dp,tz]=s.trim().split(' ');
  const[y,mo,d,h,mi,sc]=[dp.slice(0,4),dp.slice(4,6),dp.slice(6,8),dp.slice(8,10),dp.slice(10,12),dp.slice(12,14)];
  const sign=tz?tz[0]:'+',tzh=tz?tz.slice(1,3):'00',tzm=tz?tz.slice(3,5):'00';
  return new Date(`${y}-${mo}-${d}T${h}:${mi}:${sc}${sign}${tzh}:${tzm}`);
}
function fmt(d){return d.toLocaleTimeString('sw-TZ',{hour:'2-digit',minute:'2-digit'});}

function loadEPG(){
  fetch(EPG_URL).then(r=>r.text()).then(xml=>{
    const doc=new DOMParser().parseFromString(xml,'text/xml');
    const progs=Array.from(doc.getElementsByTagName('programme')).map(p=>({
      title:p.getElementsByTagName('title')[0]?.textContent||'Kipindi',
      desc:p.getElementsByTagName('desc')[0]?.textContent||'',
      start:parseXMLTV(p.getAttribute('start')),
      stop:parseXMLTV(p.getAttribute('stop'))
    })).sort((a,b)=>a.start-b.start);
    const now=new Date();
    const cur=progs.find(p=>now>=p.start&&now<p.stop);
    document.getElementById('now-title').textContent=cur?.title||'Hakuna taarifa';
    document.getElementById('now-time').textContent=cur?`${fmt(cur.start)} \u2013 ${fmt(cur.stop)}`:'';
    if(cur){
      const pct=Math.min(100,Math.round((Date.now()-cur.start)/(cur.stop-cur.start)*100));
      document.getElementById('prog-fill').style.width=pct+'%';
      document.getElementById('prog-pct').textContent=pct+'%';
      document.getElementById('prog-label').textContent=`Imesalia ${fmt(new Date(cur.stop))}`;
    }
    const today=progs.filter(p=>p.start.toDateString()===now.toDateString()).slice(0,8);
    const list=document.getElementById('epg-list');
    if(!today.length){list.innerHTML='<div style="font-size:12px;color:var(--text3)">Ongeza EPG kwenye epg.xml</div>';return;}
    list.innerHTML=today.map(p=>{
      const active=cur&&p.title===cur.title&&p.start.getTime()===cur.start.getTime();
      const past=now>p.stop;
      return`<div class="epg-item${active?' active':''}">
        <div class="epg-time${past?' past':''}">${fmt(p.start)}</div>
        <div>
          <div class="epg-name${active?' active-name':''}${past?' past':''}">${active?'▶ ':''}${p.title}</div>
          ${p.desc?`<div class="epg-desc">${p.desc.slice(0,65)}${p.desc.length>65?'...':''}</div>`:''}
          ${active?'<span class="epg-tag">SASA HIVI</span>':''}
        </div></div>`;
    }).join('');
  }).catch(()=>{
    document.getElementById('now-title').textContent='Ratiba haijapatikana';
    document.getElementById('epg-list').innerHTML='<div style="font-size:12px;color:var(--text3)">Weka EPG URL sahihi ili ratiba ionekane hapa.</div>';
  });
}
loadEPG(); setInterval(loadEPG,60000);
</script>
</body>
</html>
